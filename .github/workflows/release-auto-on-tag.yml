name: Karida Release (auto on vX.Y.Z tag)

on:
  push:
    tags:
      - 'v*.*.*'   # Only run on semver-like tags

permissions:
  contents: write

jobs:
  publish:
    uses: karida-org/.github/.github/workflows/reusable-release-auto-on-tag.yml@main
    secrets: inherit
